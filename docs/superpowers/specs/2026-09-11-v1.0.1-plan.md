# v1.0.1 — pollination-morphology (2026-09-11)

**Read this cold.** v1.0.0 shipped and was audited by a 10-judge review panel; every item
below was then verified by the coordinator against the released tree at `372e121`. You
are the executor for this repo; the coordinator owns every remote operation (merge, tag,
release). Branch `release/1.0.1-rc` from the current `master`. Push ONLY that branch.

## Scope allowlist — read twice

You MAY correct statements, titles, numbers that misquote their source, links, metadata,
and documentation structure; add committed scripts that _reproduce an existing number_;
add tests that guard a shipped claim. You MAY NOT run new experiments, change `sim/`,
retune anything, or change any registered analysis. **Every fix is a change to what is
SAID, never to what was MEASURED.** If a fix seems to need a re-run, STOP and hand back.

## Punch list — all required unless marked optional

### A. Claim-level (front page)

1. `README.md:58-59` says an evolving community "reaches 40% of the achievable ceiling".
   The document of record, `docs/2026-08-01-v1-result.md:9,51`, says **52%** after the
   head-cap correction; the doc README cites (`continuous-metric-rebaseline.md`) contains
   no 40%. Quote 52% with its source, or delete the sentence.
2. `docs/FINDINGS.md:33` — finding 1's TITLE ("worse than no floor at all") states the
   registered **secondary** (−0.211 vs the no-floor anchor). The registered primary is
   −0.221 vs the flat floor. Retitle to the primary; keep the secondary labelled secondary.
   Same fix in `tools/site-index.html:219-223` (the landing-page note). Add one sentence
   beside finding 1 disclosing what `2026-09-05-selfing-cover-prereg.md:211-217` already
   says: the primary endpoint was chosen after seeing #63.
3. Finding 2 (`docs/FINDINGS.md:59`) labels +0.289 as **`H-pool`**. The pre-registration
   `docs/2026-08-16-phenology-prereg.md` registers `H-free` (predicted NULL) and `H-link`
   only; `H-pool` is a control introduced in the result document. Relabel the number as
   the registered `H-free` contrast, state plainly that the **registered null was refuted**,
   and mark `H-pool` as a post-hoc control.
4. `docs/ROADMAP.md:1305` prints the retracted z-interval `[0.708, 0.910]` — corrected
   value `[0.706, 0.912]` (see the erratum in `hybrid-placement.md:75`). Annotate in place
   with the repo's `⚠️ CORRECTED` convention. `docs/ROADMAP.md:42-45` says the pool-size
   confound was "excluded by measurement"; FINDINGS:74-94 says `H-pool` spans zero under
   the proportional visit rule. Annotate that bullet to match FINDINGS. Update the
   "Last updated" line.
5. `README.md:189-191` says the ceiling number is "not yet in hand"; FINDINGS/ROADMAP/
   CHANGELOG carry the measured 14-vs-19 euglossine result. Replace with the result and
   its caveat.
6. `README.md:9` promises "four pre-registered results" and never lists them. Add a
   "What was measured" block: the four FINDINGS headlines (number, interval, one caveat,
   link). Move the L1/L2/τ/KDE_M lab notebook (README:48-189) under a "Foundation"
   heading after it, and define L1, L2, IBM, τ on first use.

### B. Reproducibility / links

7. `docs/2026-08-28-euglossine-ceiling.md:7` re-run command is `node
_scratch/euglossine-ceiling.js`; `_scratch/` is gitignored. Commit the parser under
   `tools/euglossine-ceiling.js` (verbatim from `_scratch/` if it still exists on the
   author's machine — ask via the handoff if not) and fix the command; if it cannot be
   recovered, change the sentence to "analysis script not shipped" and say so in FINDINGS.
8. Footers of `visit.html:951`, `population.html:1157`, `greybox.html:535` link
   `index.html`, which exists only in the built `site/`. Point "home" at
   `https://musharna.github.io/pollination-morphology/`.
9. `docs/FINDINGS.md:215` "No build step and no dependencies — Node 18 or newer": add the
   boundary — tests need Node ≥ 18.19 (tested at 18.19.1); `tools/run-*.sh` need
   `NODE_BIN`; `tools/smoke-site.py` needs Playwright + Chromium (optional, site check
   only). Mirror one Setup block in README.
10. Findings 2–4 re-run commands are 30–46-minute sweeps with no `docs/data` archive.
    Print the runtime beside each command and state which findings re-derive from an
    archive (1) and which re-run (2–4).
11. `README.md:19` cites "Mailly & Lihoreau 2025" (a thesis without a DOI per RELEASE §3.4).
    `docs/2026-07-31-groundwork-axes.md:32` names the paper _Mailly, Riotte-Lambert &
    Lihoreau 2025_ with a DOI. Cite the paper (verify the DOI resolves; add to
    `docs/dois.txt`; ghostcite clean).

### C. Hygiene / metadata

12. `docs/RELEASE-1.0.md` §6.3 waives "a non-verbatim quote" without naming it. Name the
    file:line, and either fix the quote or drop the quotation marks. §7.1 lists 9 commits
    under "Eight commits"; reconcile the 7/8/9 counts. Add a one-line banner at the top:
    "Release evidence log, not a findings document."
13. `docs/2026-07-31-groundwork-axes.md:226,423` reference `~/sim-zoo/...` — replace with
    "a sibling project (not published)".
14. `greybox.html:6` title "Grey-box v0" → drop "v0".
15. `CITATION.cff:24` `license: MIT` → keep MIT but add a `# code MIT; text, figures and
data CC-BY-4.0` comment already present? Verify; add `license-url` for both.
16. `CHANGELOG.md`: add `## [1.0.1] — 2026-09-11` listing every item above by number.
17. (optional) `docs/2026-08-01-v2-result.md:132-135` mixed-run-length table: add a visible
    warning box.

## Acceptance (record in `docs/RELEASE-1.0.1.md`)

- `node --test tests/` 363/363 · `tools/build-site.sh` + `python3 tools/smoke-site.py` exit 0
- every number changed in README/FINDINGS cites its source doc and line
- `ghostcite` on `docs/dois.txt` clean · gitleaks history + `site/` clean
- one fresh-critic pass on the rendered landing page + FINDINGS; non-waivable = a number
  disagreeing with its source, a title stating a secondary as primary, a dead link
- handoff: RC SHA, per-item disposition table (fixed / not fixed + why), evidence

Push `release/1.0.1-rc` and stop.
