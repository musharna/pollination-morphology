# Executor brief: M3a, levels.

Repo `/home/<user>/pollination-morphology`, master `0b9e08c`. Authority: spec §5 Levels, §9 M3a row
(`docs/superpowers/specs/2026-09-12-northstar-design.md:387`) and the smoke/`data-state` paragraphs; §4
for card states. Read fresh; spec wins.

1. Level select above the gene panels; choosing a level loads its configuration (N, generations,
   siteN, options as §5 lists them) into the live controls + a one-line brief; every slider stays live.
   Win = fate HELD from level 2 on; level 1's win = the separation readout; STALLED, `one lost`, FUSED,
   BOTH LOST all lose, on every level.
2. Level 2 and level 6 "no known win" copy (level 6 lists the known wins of levels 4 and 5) — authored
   text from the spec, not generated.
3. Acceptance (node tests in the fake browser, executing the page's real code): level 3 → controls read
   N 30, 35 generations, siteN 160 before running; level 1 → separation 8 reachable by sliders alone;
   level 2 at seed 1 (target 8) → `one lost` and the no-known-win copy. Time a 35-generation siteN-160
   run and assert it completes inside 60 s in a real browser (smoke); report the measured time.
4. Card 1 stays as M2 left it (grey outside its signature; the `d` switch stays). Card states must follow
   §4 precedence on every level; add one test per level that loading it does not open a card.
5. `tools/smoke-site.py`: one assertion per acceptance line.
- Branch `m3a-levels`; no push/merge; no attribution. Tests seen failing first. Full suite
  `node --test tests/` green (background, ~10 min). Keep a11y attributes; `tests/a11y.test.js` green.
  Final report: SHAs, counts, the timed run, mismatches.
